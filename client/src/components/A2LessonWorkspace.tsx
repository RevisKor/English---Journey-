import { Button } from "@/components/ui/button";
import { CourseReadingPractice } from "@/components/CourseReadingPractice";
import { CourseWritingPractice } from "@/components/CourseWritingPractice";
import { QuizPractice } from "@/components/QuizPractice";
import { trpc } from "@/lib/trpc";
import type { LessonDefinition } from "@shared/course";
import { ArrowLeft, BookOpen, CheckCircle2, Headphones, Languages, PenLine, Sparkles, Trophy } from "lucide-react";
import { useState } from "react";

type Step = 1 | 2 | 3 | 4 | 5 | 6;
type Accent = "british" | "american";

const steps: Array<{ id: Step; title: string; arabic: string; minutes: string }> = [
  { id: 1, title: "Start here", arabic: "ابدأ هنا", minutes: "5 min" },
  { id: 2, title: "Explore", arabic: "استكشف", minutes: "10 min" },
  { id: 3, title: "Notice", arabic: "لاحظ", minutes: "10 min" },
  { id: 4, title: "Build", arabic: "ابنِ", minutes: "10 min" },
  { id: 5, title: "Respond", arabic: "استجب", minutes: "15 min" },
  { id: 6, title: "Prove it", arabic: "أثبت إتقانك", minutes: "10 min" },
];

export function A2LessonWorkspace({ lesson, accent, onBack }: { lesson: LessonDefinition; accent: Accent; onBack: () => void }) {
  const [step, setStep] = useState<Step>(1);
  const [sentence, setSentence] = useState("");
  const rawSentenceMutation = trpc.ai.checkSentence.useMutation();
  const sentenceMutation = { ...rawSentenceMutation, data: rawSentenceMutation.data ? { feedbackArabic: rawSentenceMutation.data.content, correctedSentence: rawSentenceMutation.data.content } : undefined };
  const network = lesson.lexicalNetworks?.[0];
  const speak = (text: string) => {
    const utterance = new SpeechSynthesisUtterance(text);
    utterance.lang = accent === "british" ? "en-GB" : "en-US";
    window.speechSynthesis.cancel();
    window.speechSynthesis.speak(utterance);
  };
  const goNext = () => setStep((current) => Math.min(6, current + 1) as Step);

  return <main className="mx-auto max-w-[1420px] px-5 py-7 lg:px-9 lg:py-10"><button onClick={onBack} className="mb-6 inline-flex items-center gap-2 text-sm font-semibold text-[#69768d] hover:text-[#253453]"><ArrowLeft className="h-4 w-4" /> Back to A2 course</button><div className="grid gap-6 xl:grid-cols-[230px_minmax(0,1fr)_260px]"><aside className="rounded-[1.6rem] bg-[#253453] p-4 text-white xl:sticky xl:top-24 xl:h-fit"><p className="px-2 text-[10px] font-bold uppercase tracking-[.18em] text-[#f2cf71]">A2 learning route</p><h2 className="px-2 pt-2 text-lg font-bold">{lesson.title}</h2><div className="mt-5 space-y-1">{steps.map((item) => <button key={item.id} onClick={() => setStep(item.id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left transition ${step === item.id ? "bg-white/15" : "hover:bg-white/8"}`}><span className={`grid h-7 w-7 place-items-center rounded-full text-xs font-black ${step === item.id ? "bg-[#e7b84a] text-[#253453]" : "bg-white/10 text-[#c9d4e8]"}`}>{item.id}</span><span><span className="block text-sm font-semibold">{item.title}</span><span dir="rtl" className="arabic block text-xs text-[#cbd6ea]">{item.arabic} · {item.minutes}</span></span></button>)}</div></aside><section className="min-w-0"><header className="rounded-[1.7rem] border border-[#e2d8c5] bg-[#fffdf7] p-6 sm:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.17em] text-[#a2732c]">Module {lesson.moduleNumber} · Lesson {String(lesson.lessonNumber).padStart(2, "0")}</p><h1 className="mt-2 text-3xl font-bold tracking-[-.045em] text-[#253453]">{lesson.title}</h1><p dir="rtl" className="arabic mt-2 text-right text-sm text-[#718098]">{lesson.titleArabic}</p></div><span className="rounded-full bg-[#e8f3ea] px-3 py-1.5 text-xs font-bold text-[#397558]">English-first A2</span></div><p className="mt-5 max-w-2xl text-sm leading-7 text-[#58677d]">By the end, you can {lesson.learningPlan?.outcome.canDo ?? "use today’s language in a short, connected response."}</p></header><div className="mt-5 rounded-[1.5rem] border border-[#e4dccf] bg-[#fffdf7] p-5 sm:p-7">{step === 1 && <div><StepTitle icon={<Sparkles />} title="Reactivate what you already own" arabic="استدعِ ما تعرفه من A1" /><p className="mt-4 leading-7 text-[#57667c]">Before new language, connect this lesson to familiar A1 foundations. Read each sentence aloud and notice the known words that support the new idea.</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{lesson.words.slice(0, 4).map((word) => <button key={word.id} onClick={() => speak(word.exampleEN)} className="rounded-xl border border-[#e5ddcf] bg-[#faf7f0] p-4 text-left hover:border-[#bf7f2f]"><p className="font-bold text-[#253453]">{word.exampleEN}</p><p dir="rtl" className="arabic mt-2 text-right text-sm text-[#778399]">{word.exampleAR}</p></button>)}</div></div>}{step === 2 && <div><StepTitle icon={<Languages />} title="Explore a usable language network" arabic="استكشف شبكة لغوية قابلة للاستخدام" /><p className="mt-4 leading-7 text-[#57667c]">Do not memorise isolated words. Learn the chunks people actually use, then connect them to their word families.</p><div className="mt-5 rounded-2xl bg-[#eef4eb] p-5"><h3 className="font-bold text-[#315944]">{network?.theme ?? "Today’s useful language"}</h3><p dir="rtl" className="arabic mt-1 text-right text-sm text-[#56725f]">{network?.themeArabic}</p><div className="mt-4 flex flex-wrap gap-2">{network?.chunks.map((chunk) => <button key={chunk} onClick={() => speak(chunk)} className="rounded-full bg-white px-3 py-2 text-sm font-semibold text-[#397558] shadow-sm hover:bg-[#fff8dd]">{chunk} <Headphones className="ml-1 inline h-3.5 w-3.5" /></button>)}</div><p className="mt-4 text-sm leading-6 text-[#557060]">{network?.learningNote}</p></div></div>}{step === 3 && <div><StepTitle icon={<BookOpen />} title="Notice the pattern in context" arabic="لاحظ القاعدة في سياقها" /><h3 className="mt-4 text-2xl font-bold text-[#253453]">{lesson.grammar.topic}</h3><p dir="rtl" className="arabic mt-2 text-right leading-7 text-[#68758c]">{lesson.grammar.arabicComparison}</p><div className="mt-5 rounded-2xl bg-[#f8f4ec] p-5"><p className="font-mono text-sm text-[#8d6415]">{lesson.grammar.structure.positive}</p><p className="mt-3 text-sm leading-7 text-[#58677d]">{lesson.grammar.concept}</p></div><div className="mt-5 space-y-3">{lesson.grammar.examples.slice(0, 3).map((example) => <button key={example.en} onClick={() => speak(example.en)} className="block w-full rounded-xl border border-[#e6dece] p-4 text-left hover:bg-[#fcf9f1]"><p className="font-semibold">{example.en}</p><p dir="rtl" className="arabic mt-1 text-right text-sm text-[#778399]">{example.ar}</p></button>)}</div></div>}{step === 4 && <div><StepTitle icon={<PenLine />} title="Build your own connected idea" arabic="ابنِ فكرتك المتصلة" /><p className="mt-4 leading-7 text-[#57667c]">Use one useful chunk and today’s grammar to describe a real routine, choice, or experience. Your tutor will explain the most important correction in Arabic.</p><textarea value={sentence} onChange={(event) => setSentence(event.target.value)} placeholder="For example: I usually … because …" className="mt-5 min-h-32 w-full rounded-2xl border border-[#dfd6c5] bg-[#fffefb] p-4 outline-none focus:border-[#bf7f2f]" /><Button disabled={sentenceMutation.isPending || !sentence.trim()} onClick={() => sentenceMutation.mutate({ level: "A2", lessonNumber: lesson.lessonNumber, sentence })} className="mt-4 rounded-xl bg-[#253453] text-white hover:bg-[#35476d]">{sentenceMutation.isPending ? "Checking…" : "Get coaching"}</Button>{sentenceMutation.data && <div dir="rtl" className="arabic mt-4 rounded-xl bg-[#fff0bd] p-4 text-right text-sm leading-7 text-[#715820]"><p>{sentenceMutation.data.feedbackArabic}</p><p className="mt-2 font-bold">الصياغة الأفضل: {sentenceMutation.data.correctedSentence}</p></div>}</div>}{step === 5 && <div><StepTitle icon={<BookOpen />} title="Respond to a real text" arabic="استجب لنص حقيقي" /><p className="mt-4 text-sm leading-7 text-[#57667c]">Use your new chunks to understand a connected A2 text, then answer in your own English.</p><div className="mt-5"><CourseReadingPractice lesson={lesson} /></div></div>}{step === 6 && <div><StepTitle icon={<Trophy />} title="Prove it through use" arabic="أثبت إتقانك بالاستخدام" /><p className="mt-4 text-sm leading-7 text-[#57667c]">First write a purposeful response. Then pass the short lesson check to unlock the next route.</p><div className="mt-5"><CourseWritingPractice lesson={lesson} /><div className="mt-7 border-t border-[#e4dccf] pt-7"><QuizPractice lesson={lesson} /></div></div></div>}<div className="mt-7 flex justify-end"><Button onClick={goNext} disabled={step === 6} className="rounded-xl bg-[#bf7f2f] text-white hover:bg-[#a86e25]">{step === 6 ? <><CheckCircle2 className="mr-2 h-4 w-4" />Complete the check</> : "Continue"}</Button></div></div></section><aside className="space-y-4 xl:sticky xl:top-24 xl:h-fit"><div className="rounded-[1.4rem] bg-[#eef4eb] p-5"><p className="text-xs font-bold uppercase tracking-[.15em] text-[#38755b]">Your language goal</p><p className="mt-3 text-sm leading-7 text-[#4c6b58]">{lesson.learningPlan?.outcome.canDo}</p><p dir="rtl" className="arabic mt-3 text-right text-xs leading-6 text-[#587160]">{lesson.learningPlan?.outcome.canDoArabic}</p></div><div className="rounded-[1.4rem] bg-[#253453] p-5 text-white"><p className="text-xs font-bold uppercase tracking-[.15em] text-[#f2cf71]">Why A2 feels different</p><p className="mt-3 text-sm leading-7 text-[#d9e2f1]">You are now linking ideas, choosing useful chunks, and responding to meaning—not only naming words.</p></div></aside></div></main>;
}

function StepTitle({ icon, title, arabic }: { icon: React.ReactNode; title: string; arabic: string }) { return <div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8f3ea] text-[#38755b]">{icon}</span><div><h2 className="text-xl font-bold text-[#253453]">{title}</h2><p dir="rtl" className="arabic text-right text-sm text-[#78859a]">{arabic}</p></div></div>; }
